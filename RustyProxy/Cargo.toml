[package]
name = "RustyProxy"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] } # "full" habilita tudo (net, io, time, sync, rt-multi-thread)
clap = { version = "4", features = ["derive"] }
